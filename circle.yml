dependencies:
  post:
    - cp ~/commons-csv/src/site /etc/apache2/sites-available
    - a2ensite site
    - sudo service apache2 restart
test:
 post:
   - mkdir -p $CIRCLE_TEST_REPORTS/junit/
   - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
